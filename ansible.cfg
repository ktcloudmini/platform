[defaults]
inventory = aws_ec2.yml
host_key_checking = False
remote_user = ubuntu
private_key_file = /home/ubuntu/platform/ktcloud-key.pem

[inventory]
enable_plugins = aws_ec2, host_list, ini, yaml, auto